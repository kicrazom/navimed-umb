# 2026-05-23 — PLLuM-70B AWQ public release pipeline

**Scope:** Llama-PLLuM-70B AWQ W4A16 family (8 variants on HF `mozarcik/`) — public release pipeline preparation. Embargo §11.1 PUBLIC engineering (Gate 1 sanity + Gate 2 coherence). Throughput EMBARGOED §11.2/§11.3.

## What ran

- **Gate 1 sanity (fresh, 2026-05-23)** — re-run of full 8-variant sanity sweep on 2× R9700 TP=2. Confirms 2026-05-22 result identically: footprint 18.78 GiB/GPU, load 14–19 s, KV ~55k tok, max_concurrency 6.7. 6/8 PASS (instruct + chat × all versions), 2/8 base FAIL — verified `ChatTemplateResolutionError` is harness artifact (base tokenizers lack chat template; route to `/v1/completions` to resolve), not quantization defect.
- **Gate 2 coherence probe (AWQ-QA, 2026-05-23)** — 5-prompt Polish probe (fakt / polecenie / streszczenie / definicja / porównanie) on 6 sanity-PASS variants. All-flags-OK rate 4/5 to 5/5 across variants; auto-verdict `REVIEW` driven by ngram-share heuristic false-positive on short correct answers (e.g. "Stolicą Polski jest Warszawa." → 4 words, top_ngram_share 0.5 → coherent=false despite correctness). Manual spot-check (sample: instruct-2512) confirms coherent Polish across all 5 prompt categories. Vehicle-integrity OK per METHODOLOGY §8 boundary.
- No Gate 3 throughput sweep today — deferred to dedicated night session (18–36 h walltime estimate for 6 variants × N={10,25,50,100,200,500,1000} per METHODOLOGY §6).

## Public release prep (off-repo)

- Competitive scan via HF Hub query: **first public AWQ W4A16 (vLLM-native `compressed-tensors`)** of the family. GGUF alternatives (mradermacher, ~12–15k DL on i1-GGUF variants) exist for llama.cpp/ollama users — claim narrowed to "first AWQ (vLLM-native)" to remain defensible.
- mozarcik HF repos already pushed (weights only, no model cards): 8 variants. Top DL after 1–2 days: instruct-2512 (14), chat-2512 (13), instruct-2508 (9).
- Model card v1.0 prepared (3-review pipeline: ChatGPT 8/10, Gemini "professional", in-house red team with 5 CRITICAL + 8 WARNING + 8 INFO findings). All critical resolved: Llama 3.1 CL attribution + NOTICE + LICENSE + USE_POLICY files added; consortium/publisher attribution disentangled (PLLuM consortium develops, Ministry of Digital Affairs publishes under CYFRAGOVPL); paper title corrected to *"PLLuM: A Family of Polish Large Language Models"* (arXiv:2511.03823, 2025); BibTeX key uniqueness per variant; dual-platform vLLM snippets (AMD ROCm pinned `0.19.0+rocm721` vs NVIDIA portable `awq_marlin`); Intended Use hardened (Research-only, explicitly excluding clinical decision support and PHI workflows).

## Artifacts produced today

- 8 fresh sanity JSON in `environment/sanity-tests/2026-05-23-Llama-PLLuM-70B-*-awq-tp2.json` + matching `.log`
- 6 coherence-probe JSON + 6 raw-output `.txt` in `environment/coherence-probes/2026-05-23-Llama-PLLuM-70B-*-awq-coherence-{probe.json,raw.txt}`
- `calibration/clinical-pl/drug-catalog.md` (catalog of the SmPC source set used for calibration)
- Off-repo (staging): `/tmp/pllum-release/README.template.v2.md`, `generate-readmes.sh`, `upload-to-hf.sh`, `NOTICE`, `linkedin-post.md`

## Decision log

- **Re-run sanity instead of backdating yesterday's JSONs** (auto-mode classifier flagged the cp shortcut as "fabricating evidence of a fresh Gate-2 run"). Right call — fresh datapoint is methodologically clean and stack stability re-confirmed.
- **README path: 3 reviews before push** (ChatGPT, Gemini, in-house red team). Heavier upfront cost; pays off by catching the CYFRAGOVPL attribution error, the `--quantization compressed-tensors` flag-obsolescence risk on newer vLLM, and the missing Llama 3.1 CL `NOTICE`/`LICENSE` files (any single one would have made the release look unprofessional).
- **Pilot-then-bulk HF upload** (one repo first: `instruct-2412-awq`, then visual verify, then remaining 7). Standard de-risking for multi-repo public push.

## Follow-up (next session)

- Pilot HF upload + visual verify + bulk push 7 remaining
- LinkedIn publication (after HF live so links work)
- Phase 2 v0.3 sweep night job (Gate 3) — 18–36 h, EMBARGOED, paper-bound
- QAIF AIntern 2026 — 3 project submissions before 2026-05-31 (8 days)
