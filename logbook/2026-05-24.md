# 2026-05-24 — Phase 2 sweep complete (3-model PLLuM-70B AWQ)

**Scope:** Gate 3 (Phase 2 throughput scaling sweep) for 3 sanity-PASS + human-verdict-PASS variants of the Llama-PLLuM-70B AWQ W4A16 family. **PUBLIC §11.1**: this engineering note (sweep ran, walltimes, no errors, env). **EMBARGOED §11.2 / §11.3**: all throughput, latency, scaling, power, W/tok numbers per artifact, paper-bound until preprint acceptance — Polish models carry stricter embargo (§11.3).

## What ran

- **Patch + 3 human verdicts committed first** (`efb7c9c`, 2026-05-23 23:25): `sweep_one()` in `scripts/sanity_sweep_pllum70b_awq.sh` now honors a sibling `<probe>.human_verdict.json` with `verdict=PASS`, overriding auto-flag REVIEW false-positives (n-gram heuristic on short correct answers). Three verdict files added for `instruct-2508`, `chat-2512`, `instruct-2512` (the 3-model scope decided during the 2026-05-23 release session).
- **Sweep launched** `2026-05-23 23:27` with `SWEEP_NS=10,25,50,100,200,500,1000` (METHODOLOGY §6 standard grid) via `setsid nohup bash scripts/sanity_sweep_pllum70b_awq.sh --stage sweep`. `setsid` was needed because the wrapper had to survive shell disconnect — the run was unattended overnight.
- **Inhibit re-armed** before launch (`systemd-inhibit --what=sleep:idle:handle-lid-switch --mode=block`, PID 103009) so the box did not idle-suspend mid-sweep.
- **Three models swept sequentially** under the gating logic of `sweep_one`: skips on missing sanity-PASS (base-2412, base-2508 — chat-template harness artifact), skips on absent `human_verdict.json` (chat-2412, chat-2508, instruct-2412 — out of 3-model scope), and runs on the three with `human_verdict.json`. The `[note] using human_verdict.json override` log lines confirm the patch fired as designed.

## Walltime — engineering envelope (PUBLIC §11.1)

| Model | Wall time | Status |
|---|---|---|
| `Llama-PLLuM-70B-instruct-2508-awq` | 42.1 min | PASS |
| `Llama-PLLuM-70B-chat-2512-awq`     | 40.3 min | PASS |
| `Llama-PLLuM-70B-instruct-2512-awq` | 39.9 min | PASS |
| **Total** | **23:27:28 → 01:30:12 ≈ 2 h 03 min** | clean exit |

The 5–7h/model pre-sweep estimate was conservative; observed per-model walltime was ~40 min on the standard 7-point N grid (`{10, 25, 50, 100, 200, 500, 1000}`). The N point cost grew roughly with N up to the knee, then was bounded by per-batch wall time at N≥500 (longer at N=1000 due to scheduling overhead and warmup between N points).

## Thermals + power — engineering envelope (PUBLIC §11.1)

Across all three sweeps, GPU thermals stayed well below the gfx1201 throttle threshold:

- Junction peak (per-GPU max across sweep): ~92–94 °C (R9700 limit ~110 °C; ~16 °C headroom).
- Edge / memory: 62–67 °C / 30–66 °C.
- Package power per GPU under load: ~290–320 W steady, transient dips during N transitions.
- Idle after `kill_port` cleanup between models: junction returns to 31–34 °C in <1 min.
- No thermal throttling observed in any sweep.

(Per-N W_mean / W_peak / W/tok numbers are EMBARGOED §11.2 — recorded in `benchmarks/results/<model>/scaling/results_table.csv`, gitignored.)

## Artifacts produced

Each of the three sanity-PASS models has:

```
benchmarks/results/Llama-PLLuM-70B-<variant>-awq/scaling/
├── results_table.csv             # 7 rows, 19 columns per METHODOLOGY §7.3 — EMBARGOED §11.2/§11.3
├── SUMMARY.md                    # carries explicit "EMBARGO §11.2" banner + per-N table — also EMBARGOED in this form
├── thermal-runs/                 # per-N thermals.jsonl + thermals.png (with throughput numbers in caption)
└── (per-model plots: pending)    # plot_phase2_sweep.py has a schema-mismatch bug (KeyError: 'total_s'); fixable, not blocking
```

`benchmarks/results/` is gitignored at the repo root (the `.gitignore` rule is unambiguous: `benchmarks/results/`), so none of these artifacts are at risk of accidental publication via `git push`. The embargo is enforced at the filesystem boundary, not just by author discipline.

## Decision log

- **Patch over JSON edit (D1=A in release session).** Adding `human_verdict.json` override to `sweep_one` is a methodology amendment (METHODOLOGY §8: auto-flags are mechanical, human spot-check is part of the gate); editing the auto-probe JSON in place would have falsified the test artifact. The override is logged as a `[note]` line in the sweep log so every reviewer can see both the auto and human verdicts.
- **METHODOLOGY §6 standard N grid (D2).** Cross-model comparable with prior v0.2/v0.3 sweeps; the low-N cost (N=10..100 each ~3.5–3.8 min) is negligible against the per-model envelope, and the data point at N<knee is what makes the scaling curve interpretable.
- **3-model scope (D3=3 newest).** `instruct-2512`, `chat-2512`, `instruct-2508`. Top HF download counts at release time + minimal version span. The other three sanity-PASS variants (`chat-2412`, `chat-2508`, `instruct-2412`) are intentionally out of scope for this sweep — they can be promoted later with their own `human_verdict.json` if needed.
- **plot_phase2_sweep.py bug observed, not fixed tonight.** `KeyError: 'total_s'` on all three CSVs — schema drift between the plotting script and the current 19-column results table. Not a blocker; plots can be regenerated after the script is patched.
- **No auto-commit of results.** `benchmarks/results/` is gitignored, so by default `git add` would not pick anything up. Beyond that, the embargo decision is the author's per artifact — even SUMMARY.md (with its per-N table) is currently EMBARGOED §11.2, not PUBLIC §11.1. Tonight's commits stay scoped to: the patch (`efb7c9c`, before sweep) + this logbook + a sibling session-log update (after sweep).

## Follow-up (next session)

- **Fix `plot_phase2_sweep.py` `KeyError: 'total_s'`** — likely a column rename in the runner output that the plotting script has not caught up with. Then regenerate per-model plots.
- **Decide what to publish.** Per-model SUMMARY.md as-published would leak Phase 2 throughput numbers (currently EMBARGOED §11.2 + stricter §11.3 for PL models). Options: (a) keep everything in `benchmarks/results/` private until paper acceptance; (b) publish a redacted SUMMARY.md with envelope columns only (load_s, VRAM_GB, T_peak_C) and `[REDACTED §11.2]` for throughput/power until release. The METHODOLOGY §11.2 default is (a).
- **Within-family comparison (Gate 4).** With 3-model data in hand, the next analytic step per the 2026-05-22 test plan (`docs/sessions/2026-05-22-test-plan-phase2-70b-awq.md` §6) is the within-family comparison (base/instruct/chat across versions). Cross-tier (PLLuM-70B AWQ vs other tiers) needs a unified canonical plot — `plot_canonical.py` is still missing (PLAN-NEXT §1.5 anti-pattern outstanding).
- **Promote remaining 3 variants to sweep** if the within-family analysis is informative and there's value in completing the cross-version coverage. Each needs a `human_verdict.json` first (same template as the three already published).
- **QAIF AIntern 2026 — 3 project submissions before 2026-05-31** (7 days from today).

## Morning addendum — 2026-05-24 06:30–08:45

After waking up, the morning work landed three more atomic commits and closed two of the previous follow-ups:

- **`5169561` fix(plotting):** `plot_phase2_sweep.py` no longer crashes on the current CSV schema. The legacy plotter used PascalCase column names (`total_s`, `VRAM_peak_GB`, `T_peak_C`, `W_mean`, `W_peak`, `W_per_tok_Wh`, `KV_dtype`); the runner output post-2026-05 uses snake_case. Fix is a per-column `row.get(snake) or row[legacy]` fallback in `SweepRow.from_csv_row()` and `load_csv()` — backwards-compatible with v0.1/v0.2 sweep CSVs. Verified end-to-end on the three overnight 2026-05-24 PLLuM-70B AWQ sweep CSVs: all three regenerated `scaling_curve.png` + `thermal_curve.png` + `efficiency_curve.png` cleanly.

- **`c593cb4` feat(gate2):** Three additional `human_verdict.json` overrides committed for `chat-2412`, `chat-2508`, `instruct-2412`. Same template as the three from the night before, same auto-flag root cause (n-gram repetition false-positive on short correct answers, see worked example in the instruct-2512 verdict). Promoting the rest of the sanity-PASS pool to Gate 3.

- **`8aad255` feat(sweep):** `sanity_sweep_pllum70b_awq.sh` learns `DATE="${DATE:-$(date +%Y-%m-%d)}"`. Was: hardcoded `$(date +%Y-%m-%d)`. Symptom: the morning sweep refused to find any sanity / probe artifacts because they were dated 2026-05-23, not 2026-05-24, and `[skip]`ed everything. The patch lets the operator opt in to carry-over by setting `DATE` in the environment — the default behaviour is unchanged, and the override carries the same trust-the-operator standard that METHODOLOGY §8 applies to human spot-check overrides.

With those three patches in place, the **B3 sweep** (`instruct-2412`, `chat-2412`, `chat-2508` — the remaining three sanity-PASS variants) launched at 06:30 with `DATE=2026-05-23 SWEEP_NS=10,25,50,100,200,500,1000` and ran clean. Per-model walltimes:

| Variant | Wall time | Status |
|---|---|---|
| `Llama-PLLuM-70B-instruct-2412-awq` | ~42 min (06:30 → 07:12) | PASS |
| `Llama-PLLuM-70B-chat-2412-awq` | ~43 min (07:12 → 07:55) | PASS |
| `Llama-PLLuM-70B-chat-2508-awq` | ~42 min (07:55 → 08:37) | PASS |
| **Total B3** | **~2 h 07 min (06:30 → 08:37)** | clean exit |

Identical envelope to the overnight three (37.56 GB total footprint, ~55k KV @ 8192, max_concurrency 6.7, junction peak 92-94 °C — METHODOLOGY headroom intact). No errors.

After completion, the patched `plot_phase2_sweep.py` regenerated `scaling_curve.png + thermal_curve.png + efficiency_curve.png` for the three new models. **All six sanity-PASS variants now have:** `results_table.csv` (7 rows × 19 cols) + `SUMMARY.md` + per-N `thermal-runs/*.{jsonl,png}` + three publication-ready PNGs each — 18 PNGs total under `benchmarks/results/`. Everything stays gitignored per repo-wide embargo policy.

**Concrete artifact surface (PUBLIC §11.1 in this logbook; per-N numbers EMBARGOED §11.2/§11.3 in the gitignored CSVs):**

- 6 × `Llama-PLLuM-70B-{base?,instruct,chat}-{2412,2508,2512}-awq/scaling/` directories (base variants excluded — chat-template harness FAIL on Gate 1, not a quantization issue)
- 6 × `results_table.csv` (7-point N grid each, 42 measurements total)
- 6 × `SUMMARY.md` with embedded EMBARGO §11.2 banner
- 18 × matplotlib PNG plots (scaling / thermal / efficiency curves per model)
- 42 × thermal-run JSONL/PNG pairs (one per N point per model)

## Open follow-ups (refreshed evening-of-2026-05-24)

- **Within-family (Gate 4) and across-family analysis.** With six per-N sweep CSVs in hand, the within-family comparison (base/instruct/chat × 2412/2508/2512) is now data-complete. Awaiting Łukasz's review of the 18 PNGs and a decision on whether the new MI300X droplet is worth provisioning for the AQLM-2bit single-card variant (see `eval-rag/README.md` §10 decision tree).
- **`eval-rag/`** sub-project committed (`6f23bc6` + `8df8caf`) with full design (5-model RAG eval on Polish SmPC, hybrid BM25+e5 retrieval, 5-pt manual review × 3 reviewers, conditional single-card AQLM 6th model). BLOCKED on reviewer responses from Białas + Radliński; outreach drafts ready in `eval-rag/outreach/`.
- **Publication decision** for the per-model `SUMMARY.md` (still EMBARGOED §11.2/§11.3 by default; redacted-publish path option remains open).
- **Cross-tier `plot_canonical.py`** still missing (PLAN-NEXT §1.5 anti-pattern outstanding).
- **PJATK Python** — 14 tasks, deadline 2026-05-29 (5 days), mentor mode.
- **QAIF AIntern 2026** — 3 separate project submissions before 2026-05-31 (7 days).
