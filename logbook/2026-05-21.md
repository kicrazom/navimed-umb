# 2026-05-21 — Recovery Faza 2 closure + AWQ calibration corpus + v0.3 errata

## Status

Recovery wrap-up day. Quick FAIL fixes closed, the v0.3 METHODOLOGY
errata landed, and the clinical-PL AWQ calibration corpus was published
ahead of the PLLuM-70B AWQ campaign. Six commits to `main`.

## Commits

| SHA | Subject |
|---|---|
| `6f0a2bb` | docs(citation): refresh abstract for v0.3.0 |
| `53a52be` | docs(methodology): mixtral-8x7b-awq — AWQ MoE inference broken on gfx1201 |
| `ede2d37` | env(sanity): qwen3.5-9b TP=1 sanity PASS (post re-download) |
| `df9c26f` | docs(methodology): errata v1.1 — recovery-session corrections |
| `f6ab293` | docs(calibration): publish clinical-PL SmPC AWQ calibration corpus |
| `d42ef20` | test(sanity): add PLLuM-70B AWQ sanity+sweep runner |

## Recovery Faza 2 — FAIL fixes closed

- **qwen3.5-9b** — the batch FAIL was an incomplete download (3.4 MB
  metadata stub), not an image-processor defect. After full re-download
  (19 GB, 4 shards) it serves cleanly on one R9700: load 4.8 s, footprint
  17.74 GiB, KV 68,640 tokens, 27.5× max_concurrency, sanity PASS.
- **mixtral-8x7b-awq** — re-sanity confirmed the model loads fine (load
  21 s, KV 266k, 43× max_concurrency) but `AWQMoeMarlin` MoE-expert
  kernels are unsupported on gfx1201/ROCm; the WNA16 fallback produces
  degenerate all-`<unk>` output. Status `ready → inference broken`;
  excluded from the Phase 2 sweep.

## METHODOLOGY errata v1.1

transformers 5.8.1 adopted as the v0.3+ stack (`qwen3_5` arch; vLLM 0.19
accepts it empirically — 12 models PASS). PLLuM-70B #14-18 and
kimi-dev-72b-awq #21 routed to AMD compute (BF16 > 64 GB VRAM; kimi-dev
AWQ TP=2 group-alignment failure in `mlp.down_proj`). Phase 2 v0.3 scaling
sweep complete (12 models, 44 N-points) — the v0.2.0 "knee ≈ N ×
max_concurrency" model is retracted; the knee is compute-saturation-driven.
Embargo §11 unchanged.

## Clinical-PL AWQ calibration corpus published

Calibration corpus for the Phase 3 AWQ quantization of the Llama-PLLuM-70B
family — 418 chunks with a 10-field per-chunk provenance schema, derived
from EMA-published ChPL/SmPC documents (public). Drug catalog (PL/EN),
reproducible `extract_corpus.py`. Published under a source-specific
`calibration/LICENSE` (EMA reproduction terms with acknowledgment, NOT the
root CC-BY-4.0 / MIT) — raw ChPL PDFs stay local-only. Full detail in
`docs/sessions/2026-05-21-pllum-70b-awq-amd-mi300x.md`.

## PLLuM-70B AWQ sanity+sweep runner

`scripts/sanity_sweep_pllum70b_awq.sh` — sanity (vLLM serve TP=2, AITER
off, enforce-eager, 3-state response classification) for the 8
Llama-PLLuM-70B AWQ checkpoints, then a Phase 2 scaling sweep for
sanity-PASS models only. Reuses `_env.sh`, `kill_port.sh`,
`throughput_scaling_phase2.py`; embargo split per §11 (sanity JSON public,
sweep numbers gitignored). Readiness guard checks `config.json` +
`*.safetensors` so partial downloads are rejected. Produced by the code
agent team (writer + verifier).

## Next step

Run the 8-model AWQ sanity + Phase 2 throughput sweep on the local
2× R9700 pair (METHODOLOGY-compliant).
