# 2026-05-18 — Bielik-PL-11B-v3.0 Instruct TP=1 sanity (PASS)

## Status

Single sanity-test session: the Polish-focused Bielik fine-tune. One
commit to `main`.

## Commits

| SHA | Subject |
|---|---|
| `8cfbf26` | feat(env): bielik-pl-11b-v30-instruct sanity test PASS (TP=1 BF16 envelope) |

## Result

Bielik-PL-11B-v3.0-Instruct (Polish-focused fine-tune, distinct from the
multilingual `bielik-11b-v30` base) — Phase 1 envelope, PASS:

- Load 20 s wall, TP=1 BF16, enforce_eager
- VRAM 28.69 GiB GPU0 (model footprint 20.89 GiB)
- KV cache 37,616 tokens at 7.18 GiB available → 4.59× max_concurrency
- Response 6.97 s / 128 tokens, finish=length
- No errors, clean SIGTERM (no `pkill -f`)

The Polish fine-tune preserves the architecture (`LlamaForCausalLM`); KV
geometry is essentially identical to base (37,616 vs 37,584 tokens, <0.1%
delta). The 20 s vs 27 s load delta is likely page-cache warmth, not a
loader speedup — not interpreted as such without a cold-cache repeat.

## Off-protocol capability note (§8)

Cross-model PEEP expansion, capability tracking only (not an envelope
claim): 4.5B v3.0 confabulated; 11B v3.0 base (multilingual) correct;
11B v3.0 Polish-focused correct.

## Next step

Bielik-PL-11B-v3.0 TP=2 sanity, then the v0.3 batch sanity sweep.
