# 2026-05-22 — README/RELEASES split + legacy orchestrator Pike fix

## Status

Documentation hygiene pass. The root README was carrying its full release
history; that history is now a dedicated `RELEASES.md`. Two legacy
orchestrators still contained `pkill -f` cleanup blocks — fixed to the
repo kill-isolation pattern. One commit to `main`.

## Commits

| SHA | Subject |
|---|---|
| `fb1a5be` | docs(repo): clean README + RELEASES.md, fix pkill-f in legacy orchestrators |

## README → RELEASES split

Root `README.md` trimmed 252 → ~75 lines into a routing front-door; the
detailed release history (v0.1-v0.3 notes, highlights, software stack,
build timeline) extracted 1:1 into a new `RELEASES.md`. `benchmarks/README.md`
simplified 97 → ~45 lines. The README now surfaces `calibration/` and
`paper/`.

## Pike fix — legacy orchestrators

`run_plan_a.sh` and `run_plan_b_qwen72b_awq.sh` still had a `pkill -f`
cleanup block — a collateral-kill / exit-144 hazard that violates the
repo kill-isolation rule. Replaced with `scripts/kill_port.sh 8100`,
matching the v0.3 pattern.

## Next step

Run the 8-model PLLuM-70B AWQ sanity + Phase 2 sweep on 2× R9700.
