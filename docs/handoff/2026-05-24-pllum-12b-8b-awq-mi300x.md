# Handoff: PLLuM 12B + 8B AWQ on MI300X (Run-3)

**Created:** 2026-05-24
**Author:** Claude Opus 4.7 (sesja "Vault" — `~/Vaults-main`)
**For:** Claude (sesja "MI300X" — quantization runner, repo `navimed-umb`)
**Operator:** Łukasz Minarowski (root orchestrator, available na DM)
**Protocol:** **append-only**, timestamped sections, oba endy piszą do tego pliku.
**Lock convention:** brak file locking — dwie sesje piszą sekwencyjnie, kolejne sekcje zaczynamy nową `## YYYY-MM-DD HH:MM UTC — <author>`.

---

## Context — czytaj najpierw

Pełny session log: `10_Projekty/0001-navimed-umb/docs/sessions/2026-05-24-pllum-12b-8b-awq-prep.md`
(w vault) lub `docs/sessions/2026-05-24-pllum-12b-8b-awq-prep.md` (w `navimed-umb` repo,
jeśli MI300X-side ma clone).

**TL;DR:**
- Run-1 + Run-2 (2026-05-21/22) skwantowało 8× Llama-PLLuM-70B AWQ na MI300X (AMD Dev Cloud).
- Run-3 to **2 nowe modele** — rozszerzenie poza 70B na consumer GPU segment:
  1. `CYFRAGOVPL/PLLuM-12B-chat-2512` (mistral, apache-2.0)
  2. `CYFRAGOVPL/Llama-PLLuM-8B-chat-2512` (llama, llama3.1)
- Same pipeline co Run-1/2 — `llm-compressor` AWQ W4A16, calibration `corpus.jsonl` (418 chunks SmPC PL).
- "First public AWQ vLLM-native" claim verified — competitive scan zwrócił 0 hits dla obu.

## Twoja rola (MI300X-side Claude)

Jesteś sesją Claude Code zalogowaną na MI300X instance (AMD Developer Cloud) — albo
provision świeży, albo reattach do istniejącej instancji z grantu z 2026-05-21
(30-day validity → wygasa ~2026-06-20). **Twoje uprawnienia:** odpalanie skryptów na
`/scratch/`, hf CLI z PAT (write scope na `mozarcik/*`), ssh in/out.

**NIE rób:**
- Nie pushuj public na HF dopóki README + LICENSE + NOTICE + USE_POLICY nie zostaną wyrenderowane przez sesję Vault (after Gate 1 + Gate 2 PASS na R9700).
- Nie odpalaj `--quantization` flagów z `quant_llmc.py` — script jest sprawdzony, generic, NIE modyfikuj.
- Nie ruszaj plików w `/scratch/in/` dla 70B family jeśli wciąż żyją (resource conflict).
- Nie skracaj logów — pełen log z `/scratch/run3.log` należy do release artefactów (METHODOLOGY §8 vehicle-integrity).

## Tasks (kolejność)

### T1. Verify instance state

```bash
hostname; date -u
df -h /scratch
nvidia-smi || rocm-smi  # confirm 1× MI300X 192 GB HBM3
hf auth whoami           # confirm PAT z write scope
ls -la /scratch/ /scratch/in/ /scratch/out/ 2>/dev/null
ls -la /scratch/quant_llmc.py /scratch/corpus.jsonl 2>/dev/null
```

**Append result do tego pliku** pod sekcją `## <timestamp> — MI300X — T1 result`.

Jeśli `quant_llmc.py` lub `corpus.jsonl` brakuje — Łukasz scp-uje z lokalnego R9700
(`navimed-umb/calibration/quantization/quant_llmc.py` + `navimed-umb/calibration/clinical-pl/corpus.jsonl`).
Sygnalizuj brakujące pliki listą w odpowiedzi.

### T2. Get latest scripts

Łukasz scp-uje (lub Ty `git pull` jeśli MI300X-side ma navimed-umb clone):
```
navimed-umb/calibration/quantization/run_quant3.sh   → /scratch/run_quant3.sh
navimed-umb/calibration/quantization/upload3.sh      → /scratch/upload3.sh
```

`chmod +x /scratch/run_quant3.sh /scratch/upload3.sh`.

Verify SHA256 (z vault-side; uzupełnimy po pierwszym commit):
```
# TBD — sha256sum /scratch/run_quant3.sh i /scratch/upload3.sh
# vault-side wyśle expected hash w następnej sekcji
```

### T3. Run quantization

```bash
bash /scratch/run_quant3.sh
```

**Expected walltime:** ~30 min total (8B ~13 min + 12B ~19 min, sequential).
**Expected output:**
- `/scratch/out/Llama-PLLuM-8B-chat-2512-awq/` (config.json + safetensors + tokenizer files)
- `/scratch/out/PLLuM-12B-chat-2512-awq/`
- `/scratch/RUN3_COMPLETE` (sentinel file)
- `/scratch/run3.log` (full tee log)

**Capture po DONE:**
```bash
ls -la /scratch/out/Llama-PLLuM-8B-chat-2512-awq/ /scratch/out/PLLuM-12B-chat-2512-awq/
du -sh /scratch/out/*-awq
tail -50 /scratch/run3.log
sha256sum /scratch/out/Llama-PLLuM-8B-chat-2512-awq/config.json /scratch/out/PLLuM-12B-chat-2512-awq/config.json
```

**Append wszystko do tego pliku** pod `## <timestamp> — MI300X — T3 result`.
Zawrzyj: walltime per model, output sizes, czy oba RUN_COMPLETE, ostatnie 50 linii logu, sha256.

**Jeśli FAIL** dla któregokolwiek modelu:
- Nie kontynuuj na drugi model (one-shot debug)
- Pełen relevant log section (>=200 linii wokół error)
- Sugeruj root cause (memory: `feedback_navimed_test_autonomy` — Twoja diagnoza ma autonomy)
- **NIE rób retry bez decyzji vault-side** (resource cost)

### T4. Upload weights private

PO Gate 1 + Gate 2 PASS na vault-side (czyli czekaj na sygnał — sekcja
`## <timestamp> — Vault — T4 GREEN_LIGHT`):

```bash
bash /scratch/upload3.sh
```

**Expected:** 2 repo utworzone private — `mozarcik/Llama-PLLuM-8B-chat-2512-awq`,
`mozarcik/PLLuM-12B-chat-2512-awq`. Sentinel `/scratch/UPLOAD3_COMPLETE`.

**Append result** + verify HF UI że oba repo widać private + lista plików.

### T5. Stretch — 4B-gemma3 (tylko jeśli T1-T4 zielone i czas)

Eksperymentalnie z latest main `llm-compressor` + cherry-pick PR #2571
(https://github.com/vllm-project/llm-compressor/pull/2571 — "AWQ gemma3 remove input layernorm mapping").

**Risk:** garbage output. Sanity test rigorous.
**Decyzja go/no-go**: vault-side da sygnał `## <timestamp> — Vault — T5 GO` dopiero po sukcesie 12B/8B + decyzji Łukasza.

## Communication protocol

1. **Każda sesja appenduje** swoją sekcję z timestampem ISO 8601 UTC + author identity:
   ```
   ## 2026-05-24 14:23 UTC — MI300X — T3 result
   ```
2. **Nie edytuj** sekcji innej sesji (no rewrites). Jeśli była błędna, dopisz korektę w nowej sekcji.
3. **Jeśli pytanie do drugiej strony** — sekcja kończy się `> **Question for <other>:** …`.
4. **Sentinel sygnały** dla automaticznego routingu:
   - `STATUS: PENDING` — task czeka
   - `STATUS: RUNNING` — task w trakcie
   - `STATUS: DONE` — task complete
   - `STATUS: FAIL` — task failed (kontynuacja wymaga decision)
   - `STATUS: BLOCKED` — task blokuje na coś external
5. **Łukasz** może wstrzelić swoją sekcję `## <timestamp> — Operator — <topic>` z manual override.

## Error escalation

- **GPU OOM / out of memory na MI300X** dla 12B BF16 → bardzo nieprawdopodobne (12B ~24 GB << 192 GB HBM3). Jeśli pojawi się, log + halt + escalate (bug w llm-compressor).
- **Tokenizer / config mismatch** (np. mistral chat template) → log + spróbuj `quant_llmc.py` bez modyfikacji; jeśli wciąż fail, escalate.
- **HF upload 403/401** → check PAT scope na `huggingface.co/settings/tokens`, regenerate fine-grained PAT z write na `mozarcik/PLLuM-12B-chat-2512-awq` i `mozarcik/Llama-PLLuM-8B-chat-2512-awq` (memory `project_navimed_session_2026-05-20.md` ma kontext na PAT-management z 70B release).
- **Scratch full** → `df -h /scratch` najpierw, propose cleanup strategy (kasować `/scratch/in/` po quant — script już to robi).

## Vault-side checklist (jak ja czytam Twoje updates)

Ja (sesja Vault) będę polling tego pliku co kilka minut przez `git pull` (jeśli MI300X-side
commit-uje) lub przez file watch (jeśli shared filesystem). Każda Twoja sekcja jest dla mnie
sygnałem do następnego kroku:

| Twoje sekcje | Moja reakcja |
|---|---|
| T1 result + STATUS: DONE | Wyślę expected sha256 + green light na T2/T3 |
| T3 result + STATUS: DONE | Adaptuję `sanity_sweep_pllum_12b_8b_awq.sh`, koordynuję transfer weights na R9700 dla Gate 1 |
| T3 result + STATUS: FAIL | Diagnose loop — zacznę debug session z log + ewentualne next-iteration script |
| T4 result + STATUS: DONE | Renderuję 2 model cards + LICENSE + (8B: NOTICE + USE_POLICY), 3-tier review |
| T5 result + STATUS: DONE / FAIL | Stretch eval — jeśli OK, dodaj do release; jeśli garbage, skreślam 4B do Run-4 |

---

## 2026-05-24 — Vault — INIT (Claude Opus 4.7)

STATUS: PENDING (T1 waiting on MI300X-side acknowledgment)

Scripts gotowe na vault-side path:
- `/home/mozarcik/Vaults-main/10_Projekty/0001-navimed-umb/calibration/quantization/run_quant3.sh`
- `/home/mozarcik/Vaults-main/10_Projekty/0001-navimed-umb/calibration/quantization/upload3.sh`
- `/home/mozarcik/Vaults-main/10_Projekty/0001-navimed-umb/calibration/quantization/quant_llmc.py` (Run-1/2 original, no changes)
- `/home/mozarcik/Vaults-main/10_Projekty/0001-navimed-umb/calibration/clinical-pl/corpus.jsonl`

Session log: `docs/sessions/2026-05-24-pllum-12b-8b-awq-prep.md`.

**Decisions już domknięte (nie wymagają od Ciebie potwierdzenia):**
- Hardware: MI300X (AMD Dev Cloud), TP=1 quantization (192 GB headroom)
- Models: `CYFRAGOVPL/PLLuM-12B-chat-2512` + `CYFRAGOVPL/Llama-PLLuM-8B-chat-2512`
- Calibration: same corpus jak Run-1/2 (`clinical-pl-smpc-awq-calibration`)
- Stretch goal 4B-gemma3 — odracza się do późnego decision

> **Question for MI300X:** czy masz aktywny instance z grantu (~2026-05-21 + 30 dni)? Czy provision świeży?

> **Question for MI300X:** czy `/scratch/quant_llmc.py` + `/scratch/corpus.jsonl` wciąż żyją z Run-2, czy trzeba re-scp?

---

## 2026-05-24 16:30 UTC — Operator — pivot decision

**Korekta założenia.** AMD Developer Cloud / DigitalOcean MI300X to **pay-as-you-go**
(~$2-3/h), NIE darmowy grant. Vault-side oryginalny plan błędnie traktował MI300X jako
"continuing GPU credit". Faktyczny koszt 30-min runtime ~$1-2.

**Decyzja: pivot Run-3 na R9700 lokalnie** (gdzie to feasible) + **dorzucenie 4B-gemma3
do scope** (z linka HF https://huggingface.co/CYFRAGOVPL/PLLuM-4B-chat-2512).

Sequencja:
1. AQLM pre-flight (in flight, watchdog 339998, ~30 min ETA) → verdict
2. **8B AWQ local R9700** — najmniejszy z trójki, prove pipeline (peak VRAM ~32 GB,
   marginal na 1× R9700, fallback `device_map='auto'` na 2× R9700)
3. Decision point po 8B:
   - 8B clean lokalnie → **12B local** z `device_map='auto'` (peak ~50 GB → wymaga TP=2 routing dla quant)
   - 8B OOM/fragile → MI300X $5 droplet dla 12B + 4B batch
4. **4B-gemma3 stretch** — wymaga llm-compressor main + cherry-pick PR #2571 (OPEN).
   Risk garbage output. Decision GO/NO-GO po 8B+12B PASS.

**Konsekwencje dla MI300X-side Claude (Twoja rola):** STAND DOWN. Nie ma potrzeby spawn
MI300X-side sesji. Vault-side Claude (ten sam jeden ja teraz) prowadzi Run-3 lokalnie.

> **Status**: Vault-side teraz jest single source. AQLM download w toku. 8B AWQ local
> test rozpocznie się PO pre-flight verdict.

---

## 2026-05-24 16:31 UTC — Vault — RECEIVED operator pivot

STATUS: PENDING (waiting for AQLM pre-flight finish)

Acknowledged operator decision. T1-T5 (MI300X tasks) skreślone.

Adaptacja:
- `run_quant3_r9700.sh` (nowy plik) — paths `/scratch/` → `~/models/`, źródło _env.sh,
  device_map='auto' fallback dla 12B, sequential 8B → 12B
- `quant_llmc.py` — bez zmian (generic, accepts model_dir + output_dir + corpus.jsonl)
- corpus.jsonl source: `calibration/clinical-pl/corpus.jsonl` (lokalny, same hash)
- llm-compressor 0.10.0.2 w `~/venvs/autoawq/` — wystarczy dla mistral+llama (NIE wymaga
  cherry-pick dla 8B/12B; tylko 4B-gemma3 wymaga main + PR #2571)
- Release pipeline (Gate 1 → Gate 2 → model card → HF push) IDENTYCZNY co Run-2 (8244b6d)

Next vault-side action po AQLM verdict: napisz `run_quant3_r9700.sh`, odpal 8B test,
reporting back tutaj sekcją `## <ts> — Vault — 8B local result`.

---

*(Łukasz: appenduj poniżej z `## <timestamp> — Operator` jeśli chcesz override.)*
