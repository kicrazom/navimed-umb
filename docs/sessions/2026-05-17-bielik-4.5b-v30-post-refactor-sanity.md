# Bielik 4.5B v3.0 — Phase 1 envelope sanity test (post-refactor)

**Date:** 2026-05-17T21:24:24Z
**Operator:** Łukasz Minarowski <lukasz.minarowski@umb.edu.pl>
**Methodology:** [[METHODOLOGY|METHODOLOGY.md]] v1.0 §5.1, §3.1, §3.3, §8
**Embargo:** **PUBLIC** (Phase 1 envelope, lightest model in suite, smoke test only)
**Related:** [[navimed-umb]], [[broncho-nome]], [[capno-nome]]

## Methodological humility (METHODOLOGY §8)

We measure inference *throughput*, *latency*, *thermal envelope*, and
*power efficiency* under varying concurrent load. We do not measure
model quality, reasoning capability, factual accuracy, or downstream
clinical utility. Following Lerchner (2026), these are extrinsic
computational properties of the inference vehicle, not constitutive
properties of cognition. Our claims terminate at the hardware-software
interface.

W szczególności: odpowiedź modelu na prompt PEEP jest **smoke testem
pipeline'u generation**, NIE oceną poprawności klinicznej. Model
wyprodukował tokeny w oczekiwanym tempie — to jest jedyne twierdzenie
weryfikowane tutaj. Treść (m.in. zmyślone rozwinięcie akronimu PEEP)
jest poza zakresem tego pomiaru i należy do [[navimed-arena]] / per-domain
validation.

## Cel

Sprawdzić, że po refaktorze infrastruktury (2026-05-17) stack
`vllm 0.19.0+rocm721 / torch 2.10 / ROCm 7.2` nadal ładuje najlżejszy
model polski w suite NaviMed-UMB i wykonuje pojedynczą inferencję
end-to-end. Brak roszczeń o throughput skalowane (to Phase 2 sweep,
EMBARGO).

## Konfiguracja

| Parametr | Wartość |
|---|---|
| Model | `speakleash/Bielik-4.5B-v3.0-Instruct` |
| Lokalna ścieżka | `~/models/bielik-4.5b-v30` (9.3 GiB, 2 shards FP16) |
| `tensor_parallel_size` | 1 |
| `max_model_len` | 8192 |
| `gpu_memory_utilization` | 0.40 |
| `enforce_eager` | True |
| `dtype` | auto (bfloat16) |
| Test prompt (1 szt.) | `Wyjaśnij w 2 zdaniach co to PEEP w wentylacji.` |
| `max_tokens` | 128 |
| `temperature` | 0.7 |

`enforce_eager=True` ustawione defensywnie — Bielik (Llama-base) nie ma
hybrydowej atencji i mógłby działać z CUDA graphs (METHODOLOGY §3.2),
ale dla sanity testu eager redukuje ryzyko `HSA_STATUS_*`.

## Stack (causal closure per Lerchner 2026)

| Layer | Version |
|---|---|
| ROCm SMI | 4.0.0+fc0010cf6a (rocminfo: ROCk module loaded) |
| vLLM | 0.19.0 (build tag `+rocm721`) |
| PyTorch | 2.10.0+git8514f05 |
| `torch.version.hip` | 7.2.53211 |
| GPU | 2× AMD Radeon AI PRO R9700 (gfx1201, 32 GiB GDDR6) |

## Mandatory env vars (METHODOLOGY §3.1)

```bash
unset PYTORCH_ALLOC_CONF        # confirmed <unset> in JSON record
export VLLM_ROCM_USE_AITER=0
export AMD_SERIALIZE_KERNEL=1   # NOT 3 (rejected by current PyTorch)
export HIP_LAUNCH_BLOCKING=1
export ROCR_VISIBLE_DEVICES=0,1 # excludes iGPU RAPHAEL (card2)
```

Wszystkie 5 zmiennych obecne w `env_vars` JSON record.

## Wyniki (Phase 1 envelope, **PUBLIC**)

| Metric | Value |
|---|---|
| `loaded` | True |
| `load_time_sec` | 11.02 |
| Weight load time (worker) | 1.86 s (1.72 s safetensors read) |
| Model footprint (worker report) | 9.1 GiB |
| `vram_per_gpu_gb` (rocm-smi after) | `[12.586, 0.057]` GiB |
| `kv_cache_size_tokens` | 46 304 |
| `max_concurrency` (scheduler `max_num_seqs`) | 256 |
| Reported "Maximum concurrency for 8192 tokens/req" | 5.65× |
| Single-prompt generation time | 5.206 s (128 tokens) |
| Sanity throughput (output) | [EMBARGOED §11.3] (vLLM raw) |
| Sanity throughput (input) | [EMBARGOED §11.3] |
| `errors` | `[]` |

**KV cache `gpu_cache_size_bytes`** nie został zwrócony przez vLLM 0.19
introspection path dla tego config — w JSON record `kv_cache_size_gb: null`.
Tokens-level metric (46 304) wystarcza do envelope characterization.

VRAM card1=0.057 GiB potwierdza, że TP=1 trzyma cały model na card0 — TP=2
nie był testowany (z założenia, najlżejszy model nie wymaga shardingu).

## Sanity response (NIE oceniany jakościowo, §8)

```
PEEP (ang. **Pressure-Equalized Pathologically Elastic Alveoli**) – wentylowanie
z dodatnim ciśnieniem końcowo-wydechowym (wydechowym).
Odpowiada za **utrzymanie sprężystości płuc** i zapobieganie ich zapadaniu się.
...
```

Pipeline produkuje tokeny w expected format. **Twierdzenia o
poprawności medycznej są poza zakresem tej metodologii** (per §8 — model
może produkować confidently-wrong content tak samo szybko jak
carefully-cited; benchmark tego nie rozróżnia).

## Artefakty

- `environment/sanity-tests/2026-05-17-bielik-4.5b-v30-vllm-post-refactor.json` — pełny causal-closure record
- `environment/sanity-tests/2026-05-17-bielik-4.5b-v30-vllm-post-refactor.log` — raw vLLM stdout/stderr (analogicznie do `2026-05-02-tp2-qwen25-7b.log`)
- `/tmp/sanity_bielik_45b.py` — script runner (one-shot, nie commitowany do repo — odpowiednik istniejących `benchmarks/scripts/runners/sanity_*.py`)

## Background downloads (status w trakcie testu)

W tle leciały pobierania kolejnych modeli do v0.3 sweep + Kimi-Dev-72B —
**nie były blokerem** dla sanity testu (Bielik 4.5B już lokalnie). Stan
na koniec sanity:

| Model | Progress |
|---|---|
| `speakleash/Bielik-11B-v3.0-Instruct-awq` | 65 MB / ~5.5 GB (rozpoczęte) |
| `Qwen/Qwen3.5-9B` | 3.4 MB / ~18 GB (rozpoczęte) |
| `moonshotai/Kimi-Dev-72B` | 2.4 MB / ~145 GB (config tylko) |
| `CYFRAGOVPL/Llama-PLLuM-70B-chat-250801` | 64 KB / ~140 GB (metadata) |
| `CYFRAGOVPL/Llama-PLLuM-70B-base-250801` | 64 KB / ~140 GB (metadata) |
| `speakleash/Bielik-PL-11B-v3.0-Instruct` | **gated repo, dostęp odrzucony** |

Action item: poprosić o dostęp do `speakleash/Bielik-PL-11B-v3.0-Instruct`
albo skreślić z suite jeśli jest duplikatem `Bielik-11B-v3.0-Instruct`.

## Implications dla Phase 2 v0.3 sweep

- Stack po refaktorze **operacyjny** — można proceedować ze sweep
  pozostałych modeli zgodnie z `scripts/sweep_phase2_v0.3.sh` po
  zakończeniu pobierań.
- `enforce_eager=True` pozostaje konserwatywnym defaultem dla wszystkich
  modeli na gfx1201 do czasu udokumentowanej walidacji graphs path.
- KV introspection path (`gpu_cache_size_bytes`) wymaga doszlifowania
  dla vLLM 0.19 — nie blocker, ale do uzupełnienia w `sanity_bielik_45b`
  variant runner przed v0.3.

## AI assistance disclosure (Kim et al. 2026)

| Layer | Use |
|---|---|
| Dataset / data generation | Not applicable (no AI-generated data in pipeline; sanity prompt is human-authored, model output is sanity check only) |
| Experimental pipeline | Claude Code (Opus 4.7, 1M context) executed sanity test orchestration on 2026-05-17 — generated probe script `/tmp/sanity_bielik_45b.py` based on existing `sanity_bielik_11b.py`, ran wrapper with mandatory env vars, parsed results |
| Reporting | Claude Code generated this session log; all numerical claims verified against raw JSON record at `environment/sanity-tests/2026-05-17-bielik-4.5b-v30-vllm-post-refactor.json` and raw vLLM log at same prefix `.log` |

## Embargo classification

- **PUBLIC engineering:** envelope numbers (load time, VRAM footprint,
  KV cache tokens, max concurrency) — zgodne z METHODOLOGY §11.1
- **NIE PUBLIC (out of scope):** żadne scaling, latency P50/P95/P99, ani
  cross-model comparison — ta sesja nie produkowała Phase 2 artefaktów
